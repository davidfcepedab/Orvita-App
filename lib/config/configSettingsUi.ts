/** Botones de acción en Configuración → Conexiones (píldora, coherente en Google/Hevy/salud/banca). */
export const configConnectionActionClass =
  "inline-flex min-h-9 min-w-0 max-w-full select-none items-center justify-center gap-1.5 rounded-full border px-3 py-1.5 text-[11px] font-semibold shadow-sm transition [transition-property:box-shadow,transform,opacity] hover:shadow-md active:scale-[0.99] disabled:pointer-events-none disabled:opacity-40 sm:min-h-10 sm:px-3.5 sm:py-2 sm:text-xs"

/** Etiqueta de sección secundaria (p. ej. bajo un H2) — misma pauta HIG: título, pie en bajo contraste. */
export const configSettingsSectionKickerClass =
  "m-0 px-4 pb-1.5 pt-2.5 text-[10px] font-semibold uppercase tracking-[0.12em] sm:px-5 sm:pt-3"
